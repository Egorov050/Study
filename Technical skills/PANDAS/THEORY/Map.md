Дает возможность замапить значения. 

```python 

import pandas as pd

df = pd.DataFrame({'тип': ['a', 'b', 'c']})

# Создаем новый столбец с заменёнными значениями
df['тип_название'] = df['тип'].map({'a': 'Тип A', 'b': 'Тип B'})

```

